rootProject.name = "product-versions"

pluginManagement {
    repositories {
        maven(url = "https://maven-central-eu.storage-download.googleapis.com/maven2")
        maven(url = "https://cache-redirector.jetbrains.com/plugins.gradle.org")
        gradlePluginPortal()
    }
}
